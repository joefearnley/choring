{
  "name": "choring-frontend-build",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.28.1"
  },
  "scripts": {
    "build": "esbuild static/frontend/weekly.jsx static/frontend/completed.jsx --outdir=static/frontend/build --bundle --loader:.jsx=jsx --minify --format=iife"
  }
}
